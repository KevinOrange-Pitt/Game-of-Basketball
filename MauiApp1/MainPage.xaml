<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <VerticalStackLayout
        Padding="20"
        Spacing="12">
        <Label
            Text="Azure SQL Viewer"
            Style="{StaticResource Headline}" />

        <Button
            Text="Show Players"
            Clicked="OnShowDatabaseClicked"
            HorizontalOptions="Fill" />

        <CollectionView ItemsSource="{Binding Items}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="50,*,*,70" Padding="0,8">
                        <Label Grid.Column="0"
                               Text="{Binding Id}"
                               FontAttributes="Bold" />
                        <Label Grid.Column="1"
                               Text="{Binding PlayerName}" />
                        <Label Grid.Column="2"
                               Text="{Binding Team}" />
                        <Label Grid.Column="3"
                               Text="{Binding Points}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>
