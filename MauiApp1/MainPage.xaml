<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage">

    <VerticalStackLayout
        Padding="24"
        Spacing="14">
        <Label
            Text="MAUI Hello World"
            Style="{StaticResource Headline}" />

        <Label
            Text="Milestone 1: MAUI -> API -> SQL"
            FontAttributes="Bold" />

        <Label
            Text="{Binding StatusMessage}" />

        <Button
            Text="Load Milestone Record"
            Clicked="OnLoadMilestoneClicked"
            HorizontalOptions="Fill" />

        <Label Text="{Binding PlayerLine}" />
        <Label Text="{Binding TeamLine}" />
        <Label Text="{Binding PointsLine}" />
    </VerticalStackLayout>

</ContentPage>
